# Fonts

Place production font files here before rendering:

- `BebasNeue-Regular.woff2`
- `JetBrainsMono-Regular.woff2`
- `JetBrainsMono-Bold.woff2`

The Remotion template is already wired to these local paths. Keeping fonts local
prevents the preview and final MP4 from drifting apart during server rendering.
